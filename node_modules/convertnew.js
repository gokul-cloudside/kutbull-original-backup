var swaggerConverter = require('swagger-converter');
var apiDeclarations = {
  '/api': require('api.json')
};

var swagger2Document = swaggerConverter.convert('http://dev.dataglen.com/swagger/api-docs/', apiDeclarations);
console.log(JSON.stringify(swagger2Document, null, 2));